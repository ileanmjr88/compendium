# Failure mode: entry source is not lockfile.SourceRegistry ("registry").
# Validate must return *ValidationError (source must be "registry").
[meta]
schema = 1
project = "wordNebula"
platforms = ["darwin-arm64"]

[[language]]
name = "go"
version = "1.22.5"
spec = "1.22"
source = "public"

[[language.artifacts]]
platform = "darwin"
arch = "arm64"
url = "https://example.com/go.tar.gz"
checksum = "sha256:abc123"
size = 123
strip = 1
