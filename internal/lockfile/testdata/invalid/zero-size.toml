# Failure mode: artifact size is zero.
# Validate must return *ValidationError (artifact size must be non-zero).
[meta]
schema = 1
project = "wordNebula"
platforms = ["darwin-arm64"]

[[language]]
name = "go"
version = "1.22.5"
spec = "1.22"
source = "registry"

[[language.artifacts]]
platform = "darwin"
arch = "arm64"
url = "https://example.com/go.tar.gz"
checksum = "sha256:abc123"
size = 0
strip = 1
